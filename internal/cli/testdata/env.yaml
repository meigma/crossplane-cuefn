# Merged environment for the render command test. tier=production so the
# rendered tier differs from the module's #Spec/transform default of "unset",
# proving --env flows into the module's input.environment.
tier: production
