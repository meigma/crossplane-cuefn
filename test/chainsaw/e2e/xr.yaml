# The primary XR for the reconcile assertions. replicas is set explicitly (2) so
# the composed Deployment's replicas prove the spec flows through the engine.
apiVersion: platform.meigma.io/v1alpha1
kind: XApp
metadata:
  name: demo
  namespace: default
spec:
  image: ghcr.io/stefanprodan/podinfo:6.7.0
  replicas: 2
