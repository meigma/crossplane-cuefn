---
id: 008
title: Native observed-resource readiness
started: 2026-07-16
---

## 2026-07-16 13:19 — Kickoff
Goal for the session: Begin the reviewed Catalyst proposal to add a backward-compatible, explicitly opted-in `out.input.observedResources` path, carrying full observed composed objects through the contract, function adapter, render engine, standalone and Crossplane render flows, compatibility coverage, live readiness proof, documentation, and contract/product releases.
Current state of the world: The proposal at `/Users/josh/work/catalyst-infra/.wt/journal-jmgilman/.journal/044/cuefn-observed-resources-implementation-proposal.md` has been reviewed. Local `master` and `origin/master` both point to `9a4f3ca` (product `v0.1.3`), contract `v0.2.0` remains the latest contract release, and release PR #55 for product `v0.1.4` is open and unmerged. The proposal's source- and SDK-level assumptions still need code-level revalidation before edits. Sessions 001–007 are closed and the personal journal worktree is synced and clean; the main checkout's pre-existing untracked `.claude/` and `xr.yaml` are out of scope and must be preserved.
Plan: Start with a focused prototype proving explicit CUE field opt-in plus lossless observed-object round-trip, then build the smallest vertical contract/engine/adapter slice and extend it through CLI and render integration. Once behavior is stable, add the dedicated readiness fixture and live two-XR proof, tighten compatibility and failure-path coverage, update compact docs, and carry the implementation through exact-head CI and published-artifact verification.

## 2026-07-16 13:47 — Observed-resource vertical slice proven
Created Worktrunk branch `feat/observed-resource-readiness` from `origin/master` at `9a4f3ca`. The contract now exposes an open optional observed-resource map; the renderer fills it only when the module materializes a regular field, sends a concrete `{}` on the first pass, and leaves optional-only and legacy closed modules byte-for-byte unchanged. The function adapter carries full observed composed object bodies under stable resource keys while excluding connection details. Standalone `cuefn render` now accepts raw file/directory snapshots via `--observed-resources`, validates the standard composition-resource-name annotation, and rejects missing, empty, or duplicate stable keys.

Focused contract/render/function/CLI/integration tests and the full Go lint configuration pass. `moon run root:render-test --summary minimal` also passed and visibly executed all three real Crossplane render cases: normal render, required-resource fixpoint, and observed-resource delivery. The observed case used a physical Deployment name distinct from the `workload` annotation and reflected an open vendor status field, proving stable-key and full-object fidelity across the real protocol. Next: build the dedicated Job/Deployment/ConfigMap readiness module, offline transition matrix, and two-XR live Chainsaw proof.

## 2026-07-16 14:20 — Review-ready implementation
Completed the proposal's implementation phase on `feat/observed-resource-readiness` and opened ready PR #64, `feat: expose observed composed resources to CUE modules`, at exact head `47b88ae70b838d84f1ec1d34ab6c3c03deb161ce`. The contract, function adapter, and render engine now provide explicit regular-field opt-in, first-pass `{}` normalization, stable-key addressing, full unstructured object fidelity, and deliberate exclusion of connection details. Standalone render accepts validated file, directory, and multi-document snapshots through `--observed-resources`. Legacy and optional-only modules retain byte-for-byte behavior.

Added the dedicated import-free XReadiness example with conservative Job, Deployment, and conditionless ConfigMap predicates, offline healthy/stale/failure/wrong-identity coverage, real pending-to-ready Crossplane render snapshots, and a live two-XR kind/Chainsaw test. The live test proves distinct migration and workload gates become ready through normal reconciliation after changing only XR spec gates; it also confirms ConfigMap identity through UID without relying on a status block. Added focused how-to and reference documentation for opt-in behavior, stable versus physical names, full-object and trust-boundary semantics, readiness predicates, and snapshot-driven offline rendering.

Local validation passed: `moon run root:check --summary minimal`, `moon run root:render-test --summary minimal`, `mise run image-local`, `moon run root:e2e-test --summary minimal`, full `golangci-lint`, contract CUE validation, Chainsaw lint, and `actionlint`. Three independent adversarial reviews found no remaining issues after tightening identity-negative cases, failing opted-in integration suites when prerequisites are absent, pinning and verifying PR-head checkout SHAs, exercising a pending-to-ready real render transition, and correcting the unspecified-readiness documentation.

Hosted validation passed on the exact PR head: CI run 29535200190, Integration run 29535200199, and E2E run 29535200288. Integration logs show `TestObservedResources_CrossplaneRender`, `TestRenderLoop_CrossplaneRender`, and `TestRequiredResources_CrossplaneRender` all ran and passed. E2E logs show `TestE2E_Kind/observed_resource_readiness` and Chainsaw case `e2e-observed-resource-readiness` ran and passed. The release dry-run detector passed; artifact jobs were correctly skipped because this PR does not change release configuration or versions. PR #64 is open, non-draft, and merge-clean, and the feature worktree is clean.

Paused for review before merge as requested. Existing release PR #55 remains open and untouched. After approval and merge, the remaining proposal phase is to publish contract v0.3.0, ship the product release (resolving the existing v0.1.4 release PR as appropriate), and prove the behavior from published artifacts rather than source checkouts.

## 2026-07-16 14:59 — Crossplane directory-loader review fix
Addressed review feedback that standalone `cuefn render --observed-resources` recursively consumed nested YAML and treated an empty directory as `{}`, unlike Crossplane CLI v2.3.3. Verified the exact upstream implementation at `crossplane/cli` tag v2.3.3: its shared loader uses a sorted immediate `ReadDir`, accepts only lowercase `.yaml`/`.yml` directory entries, ignores subdirectories, and errors when no immediate YAML file exists.

Commit `4b32e1d` replaces the shared recursive loader with those semantics, aligning both observed and required resource snapshot flags while preserving unset flags, direct files, multi-document streams, and present-but-empty YAML files. Added focused tests proving an empty directory errors and a nested stale observed-resource fixture is ignored, plus CLI reference documentation for the directory contract. Focused loader tests passed with the race detector; `moon run root:check --summary minimal` passed; an independent patch review found no remaining issues.

PR #64 is again open, non-draft, merge-clean, and paused for review at exact head `4b32e1d2a8c2190bec5b702797d80f0719e18ae4`. Hosted exact-head checks passed with checkout-SHA verification: CI run 29537720840, Integration run 29537720713, and E2E run 29537720817. Existing release PR #55 remains untouched, and no merge or release action was taken.

## 2026-07-16 15:10 — Implementation merged
With developer approval, squash-merged PR #64, `feat: expose observed composed resources to CUE modules`, pinned to reviewed head `4b32e1d2a8c2190bec5b702797d80f0719e18ae4`. GitHub created master commit `60ff2bf8da9cee7ee035beddadfd08d781655f72`. Fast-forwarded the local master checkout to the exact remote commit while preserving the pre-existing untracked `.claude/` and `xr.yaml`, then removed the integrated Worktrunk worktree plus local and remote `feat/observed-resource-readiness` branch.

Post-merge validation passed on the exact master commit: CI run 29538206384, Integration run 29538202675, E2E run 29538206113, and GitHub Pages run 29538206182. Each of CI, Integration, and E2E passed checkout-SHA verification. Release Please run 29538203105 succeeded; it opened PR #65 for contract 0.3.0 and refreshed existing product release PR #55 for 0.1.4. Neither release PR was merged and no release was published. Session remains open for the contract and product release plus published-artifact proof phase.

## 2026-07-16 17:36 — Contract and product releases proven
With developer approval, merged contract release PR #65 at reviewed head `0c1c6c`, producing master/tag commit `67e636ec629b80b4ebb907ac34373de389858430`. Contract release run 29539156927 succeeded, and CUE Central serves `github.com/meigma/crossplane-cuefn/contract@v0.3.0` at digest `sha256:a84e8381392580bdb30132860550d7e38ee491d52215cb1f4822e3c4f22d030e`. Its GitHub release intentionally remains draft because Central is the contract distribution surface and publishing the draft would invoke the product-only distributor.

Repaired the existing product release PR #55, verified every hosted check on exact head `b2e26fc5262298ff7202eabe78782f310ed3529c`, and merged it as `95dfead77e6cee91abcac78dd055c77c94c0e593`, creating tag `v0.1.4`. The original tag-push release run 29540656555 initially failed only at GitHub asset upload during the documented API incident. Merged PR #66, `fix(release): retry transient asset uploads`, as `8eebd929b75346d5a5672a5c858546c562b5b560` to add bounded upload retries. After GitHub reported recovery, a workflow-dispatch rerun produced branch-context attestations and was canceled before publication; rerunning the original tag-push workflow preserved the correct `refs/tags/v0.1.4` and `95dfead` provenance. Attempt 3 of run 29540656555 then passed all ten release jobs.

Independently verified the draft before publication: exactly five archives, five SPDX 2.3 SBOMs, and `checksums.txt`; every checksum passed; all ten archive/SBOM attestations bound to the tag, exact source commit, reusable attestation workflow, and GitHub-hosted runners. The host binary reports `cuefn 0.1.4 (95dfead...)`. The immutable runtime image is `ghcr.io/meigma/crossplane-cuefn@sha256:d3aaabb98b02824c8168fd58b03deaf0679fef5a29e0754b569e39c7a3abc14d`; the Function package is `ghcr.io/meigma/function-cuefn@sha256:a29669e3df0dbd4541d247509f74dd30ffcecb1d1dcc44b14e53c7fa036b8704`. Both multi-architecture artifacts passed strict GitHub attestation and cosign identity verification, and Crossplane 2.3.3 successfully extracted and validated the Function package metadata and input CRD.

Published the product release at `https://github.com/meigma/crossplane-cuefn/releases/tag/v0.1.4`. Release Distribute run 29544904576 passed all steps; the four Homebrew and one Scoop URLs return HTTP 200 and their hashes exactly match the release checksums. The resulting distribution commits are `5dbfe5d9ea259efcf01dd4a04fad8be1eba80cdd` in `meigma/homebrew-tap` and `c0ec1efd55d3bfc4dcb96d510ab2941266d7113a` in `meigma/scoop-bucket`.

Completed a disposable published-pair proof with fresh caches: CUE Central resolved contract v0.3.0 at the expected digest, the runtime and Function were pulled by their immutable published digests, and the live FunctionRevision resolved the exact package digest. The focused `e2e-observed-resource-readiness` Chainsaw case passed both initial `Ready=False` states and spec-only transitions to `Ready=True`; the complete `TestE2E_Kind` suite also passed. The harness removed its temporary cluster and registries, restored the temporary source patch, and left the unrelated pre-existing Kind cluster untouched.

Removed the integrated product-release, retry, and abandoned direct-upload Worktrunk worktrees and branches. The main checkout retains only the developer-owned untracked `.claude/` and `xr.yaml`. The recovered Release Please run 29541869044 attempt 2 succeeded and opened routine next-release PR #67 for v0.1.5; it remains open and was not merged. Session 008 remains open and paused for developer review.

## 2026-07-16 17:43 — Close
Closed session 008 with the goal fully met. Merged implementation PR #64, contract release PR #65, product release PR #55, and release-retry PR #66; local `master` is fast-forwarded to `8eebd929b75346d5a5672a5c858546c562b5b560`, all implementation worktrees are removed, and no journal files are tracked on the default branch. Contract v0.3.0 is public in CUE Central, product v0.1.4 and its distribution are public and verified, and the published-pair readiness proof passed. Routine v0.1.5 release PR #67 remains open as a future release decision. Wrote `.journal/008/SUMMARY.md`, marked the index entry complete, and added the durable observed-resource and release-recovery rules to `.journal/TECH_NOTES.md`; developer-owned `.claude/` and `xr.yaml` remain untouched.
