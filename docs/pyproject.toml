[project]
name = "crossplane-cuefn-docs"
version = "0.0.0"
description = "MkDocs documentation site for crossplane-cuefn."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
